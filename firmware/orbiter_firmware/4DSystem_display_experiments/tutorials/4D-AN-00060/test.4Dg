#platform "Gen4-uLCD-70DCT-CLB"


// Program Skeleton 1.3 generated 2019-03-02 13:09:19

#inherit "4DGL_16bitColours.fnc"

#inherit "VisualConst.inc"

#inherit "testConst.inc"

func main()
//  var hstrings ; // Handle to access uSD strings, uncomment if required
//  var hFontx ;   // Handle to access uSD fonts, uncomment if required and change n to font number
//  Uncomment the following if uSD images, fonts or strings used.
/*
    putstr("Mounting...\n");
    if (!(file_Mount()))
        while(!(file_Mount()))
            putstr("Drive not mounted...");
            pause(200);
            gfx_Cls();
            pause(200);
        wend
    endif
//    gfx_TransparentColour(0x0020);    // uncomment if transparency required
//    gfx_Transparency(ON);             // uncomment if transparency required

//  hFontn := file_LoadImageControl("test.dan", "test.gcn", 1); // Open handle to access uSD fonts, uncomment if required and change n to font number dropping a and c if > 9
//  hstrings := file_Open("test.txf", 'r') ; // Open handle to access uSD strings, uncomment if required
    hndl := file_LoadImageControl("test.dat", "test.gci", 1);
*/

    gfx_Set(SCREEN_MODE,LANDSCAPE) ;

    var ax[4], ay[4];   //array declaration
    var bx[4], by[4];
    repeat

    ax[0] := 10;    ay[0] := 10;
    ax[1] := 30;    ay[1] := 10;
    ax[2] := 30;    ay[2] := 20;
    ax[3] := 10;    ay[3] := 20;

    bx[0] := 50;    by[0] := 10;
    bx[1] := 80;    by[1] := 10;
    bx[2] := 80;    by[2] := 20;
    bx[3] := 50;    by[3] := 20;

    //gfx_Cls();
    gfx_PolygonFilled(4, ax, ay, WHITE);
    gfx_Polygon(4, ax, ay, GRAY);
    gfx_PolygonFilled(4, bx, by, BLACK);
    gfx_Polygon(4, bx, by, GRAY);
    pause(10);

    //gfx_Cls();
    gfx_PolygonFilled(4, ax, ay, BLACK);
    gfx_Polygon(4, ax, ay, GRAY);
    gfx_PolygonFilled(4, bx, by, WHITE);
    gfx_Polygon(4, bx, by, GRAY);
    pause(10);

    forever


endfunc

